# Changelog

## v2.2.0 (2026-08-25)

- 新增可关闭的随机自动走动，使用分层二维骨骼生成的左右 16 帧行走动画；锁定身体、头发、胳膊和尾巴，仅让双腿自然交替
- 将左右行走序列分别封装为单个无损多帧 WebP，并清理运行目录中的逐帧 PNG 和旧版走路中间资源
- 修复走动期间被天气、提醒、电脑状态等场景动作替换的问题；行走动画现在保持最高显示优先级，场景动作会在停下后按完整时长播放
- 修复行走角色的上半身色差、呆毛缺失、尾巴和刘海缺帧、腿部关节露边及步态不交替等视觉问题
- 将六张待机眨眼帧合并为单个多帧 WebP，在保持原有 1160 ms 动画节奏的同时减少运行资源数量
- 新增番茄钟，支持自定义专注/休息时长、开始、暂停和重置
- 新增久坐、喝水和下班提醒，并利用系统空闲时间自动重置久坐计时
- 新增手动及定时安静模式，暂停随机动作、闲聊、电脑状态联动和非关键提醒
- 新增角色缩放、透明度和位置锁定，缩放后自动限制在可见桌面范围内
- 新增带格式、大小和字段校验的 JSON 存档导入导出，天气 API Key 不参与导出
- 补全饱食度闭环：设置面板显示饱食度，低饱食自动反馈，投喂恢复，饱食时继续投喂降低收益
- 优化设置面板：动作按触发场景分组，养成数据、任务、称号及开关状态改为局部实时刷新
- 新增可双击运行的 `build-portable.bat`，在构建前检查 npm、项目文件和依赖
- 修复宠物隐藏、安静模式、系统锁定或挂起后仍查询电脑状态的问题，并在暂停时终止尚未完成的查询
- 将开发环境基线统一为 Node.js 22.12 以上，与 Electron 43 的安装要求保持一致
- 新增状态机、小游戏、养成、任务、签到、天气和电脑监控策略的行为测试及覆盖率脚本
- 隐藏、锁屏或挂起时暂停鼠标探测、系统空闲采样、置顶巡检、待机循环和小游戏循环，减少后台唤醒
- 将设置数据、存储模型、电脑监控策略和诊断日志拆为独立模块，降低主文件耦合
- 存档升级为 v2 字段模型，兼容 v1 导入，增加字段白名单、类型范围校验、损坏值修复和导入失败回滚
- 新增本地滚动诊断日志、托盘日志入口、渲染进程异常记录和限次自动恢复
- 新增 ESLint、Prettier、统一语法检查、官方源依赖审计及 Windows GitHub Actions 构建门禁

## v2.1.1 (2026-08-21)

- 修复设置面板好感度、等级等养成数据不会实时刷新的问题，现在互动后打开设置即可看到最新数值，无需重启应用
- 新增 5 种每日任务：夸夸鲸鱼娘、玩一局小游戏、赢一局小游戏、戳肚子、摸尾巴，每日从 7 个候选中随机抽取 2 个搭配固定签到，不再每天重复相同任务
- 为夸夸、戳肚子、摸尾巴和小游戏结算补充每日任务进度追踪信号
- 配置 electron-builder 国内镜像（npmmirror）并预缓存 winCodeSign 签名工具，打包时不再依赖 GitHub 网络连接

## v2.1.0 (2026-08-21)

- 新增静止状态眨眼循环动画，按睁眼、半闭眼、闭眼、闭眼、半闭眼、睁眼顺序播放
- 统一六帧动画的 512×512 尺寸、透明背景与角色定位，减少播放时的位移跳动
- 清理角色尾巴、头发及身体缝隙中的残留背景色
- 将静止状态眨眼帧接入项目资源目录，保持现有静止状态播放逻辑兼容

## v2.0.0 (2026-08-20)

- 新增“电脑状态联动”总开关
- 新增前台应用分类联动，覆盖 IDE、浏览器、办公、音乐、会议、终端、设计和游戏场景
- 新增 CPU、内存高负载联动，并通过连续采样和冷却机制减少误触发
- 新增低电量、接通/拔掉电源、网络断开/恢复联动
- 为全部电脑状态事件增加专属动作和多条随机台词
- 新增系统锁定、睡眠、恢复及全局键鼠空闲状态联动
- 扩充桌面随机动作池，并为随机动作补齐匹配台词和防重复逻辑
- 修复透明鼠标穿透窗口首次启动时可能被 IDEA、浏览器等前台窗口遮挡的问题
- 保持非全屏浮动置顶，避免触发 Windows 自动“请勿打扰”规则
- 明确隐私边界：不读取窗口标题、键盘输入、文件或网页内容

## v1.0.0 (2026-08-20)

- 首个独立 Windows 桌面版，不依赖 DeepSeek Harness
- 透明置顶、鼠标穿透、系统托盘、开机启动
- 支持多显示器虚拟桌面与跨屏拖动
- 设置面板支持拖动、边界限制与位置记忆
- 完整养成、签到、任务、称号、成就和两种小游戏
- 内置 89 个动作资源与动作预览
- 天气效果不绘制颜色蒙版，仅保留局部雨、雪、风粒子和角色天气动作
- 提供便携版、安装版构建命令及 GitHub Actions Windows 自动构建
